# Copy to .env and set for your environment. Expo inlines EXPO_PUBLIC_* vars at build time.
# Physical phone on Expo Go: use your dev machine's LAN IP, not localhost.
# After EC2 deploy: EXPO_PUBLIC_API_BASE_URL=http://<EC2_PUBLIC_IP>
EXPO_PUBLIC_API_BASE_URL=http://localhost:4000
