PORT=4000
DB_PATH=./data/onevoice.db
UPLOAD_DIR=./uploads

# Get a free-tier key at https://aistudio.google.com/apikey
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

# Simple shared-secret auth for the admin dashboard (MVP only — replace before any real deployment)
ADMIN_USER=admin
ADMIN_PASSWORD=changeme
